{{>header}}
{{>navbar}}
    <form method="post">
        <div class="mb-3">
            <input placeholder="Title" name="title" class="form-control"
            value="{{data.title}}" required>
        </div>
        <div class="mb-3">
            <textarea placeholder="Comment" name="content" class="form-control"
            value="{{data.content}}" required></textarea>
        </div>
        <div class="mb-3">
            <button class="btn btn-primary">Add article</button>
        </div>
    </form>